# 프로필 재집필 기준

## 구성과 목적

README는 자기소개 → 경험과 일하는 방식 → 공개 프로젝트 → 기술 스택 → 연락처 → GitHub 활동 순서로 구성한다. 직함 나열보다 무엇을 하고 어떻게 판단하는지가 먼저 드러나도록 쓴다. 섹션 전체를 다시 집필할 수 있으며, 부분 수정 요청에서는 요청 범위만 바꾼다. HTML 마커는 섹션 경계다.

## 사실의 근거

- 자기소개·경력·기술·연락처는 로컬 `~/resume` 프로젝트의 최신 정련 이력서를 우선한다. 현재 경로는 `03_set/이력서.md`다. 경로 변경 시 실제 디렉터리를 확인하고, 예전 `01_source` 경로를 가정하지 않는다.
- 이력서의 직무별 플레이스홀더와 내부 편집 지침은 프로필에 옮기지 않는다.
- 경력 개월 수에 단순히 오늘까지의 기간을 더하지 않는다. 최신 근무 이력이 없으면 연차를 생략한다.
- 프로젝트는 GitHub의 공개 여부·fork·archive·pushedAt과 공개 README·구현을 확인한다. 로컬 변경을 원격에 반영된 기능으로 소개하지 않는다.
- 최근 반영일은 pushedAt의 UTC 날짜다. 최근 push만으로 운영 중·완성·출시를 주장하지 않는다.

## 프로젝트 선정·공개 기준

- 공개·비-fork·비-archived 저장소 중 최근 6개월 내 변경이 있는 작업을 중심으로 선정한다.
- 각 프로젝트에 Summary와 구현·탐구 내용을 표로 제공하고, 전체 활동을 짧은 문단으로 요약한다.
- 빈 저장소, 프로필 자신, 이력서, 단순 환경 설정은 대표 프로젝트에서 제외한다. 장기 미갱신 저장소는 필요할 때 학습·보관 기록으로 명시한다.
- 비공개 저장소 이름·주소·고객사·회사 자산명·구체적인 내부 정보는 공개 산출물에 넣지 않는다. 기존에 노출되었더라도 새 글에서는 제거한다.
- 실제 스택과 소개 스택의 근거를 구분한다. 사용 경험을 근거 없이 주력으로 승격하지 않는다.
- 구직 상태와 현재 직책을 추정하지 않는다. 이메일·기존 공개 연락 링크만 사용한다.

## 통계 유지보수

`scripts/update_stats.py`는 GitHub GraphQL 기여 캘린더와 공개 저장소 API를 사용한다. 주 카드는 최근 365일(UTC, 오늘 포함)의 전체 기여·커밋·생성 PR·PR 리뷰·생성 이슈·활동일 수·최근 30일 기여를 보여준다. 저장소 수·스타·포크로 활동 지표를 대체하지 않는다. 언어별 저장소 수는 보조 카드다.

- GitHub의 기여 인정 규칙을 따른다. 전체 git 커밋 수나 전체 기간 누적 수라고 표현하지 않는다. 오늘 수치는 집계 시점까지다.
- 전체 기여에는 공개 설정에 따른 익명 비공개 기여가 포함될 수 있다. 유형별 수치는 토큰의 조회 가능 범위이며 전체 기여와 단순 합산이 일치하지 않을 수 있다.
- 기본 토큰은 GitHub Actions의 `github.token`이다. 비공개 기여 접근이 필요하면 선택적으로 `PROFILE_STATS_TOKEN` secret을 설정한다. GitHub 문서에 따르면 private/internal 기여에는 `read:user` 범위가 필요하다. 토큰이나 비공개 저장소 식별 정보는 산출물에 넣지 않는다.
- 토큰 권한 오류·API 실패·GraphQL errors·누락된 지표·불완전한 날짜별 캘린더는 실패로 처리한다. 누락을 0으로 바꾸거나 공개 데이터로 몰래 전환하지 않는다.
- 두 API 호출·집계·SVG 검증이 완료된 뒤 이미지를 교체한다. 갱신 실패 시 마지막 정상 이미지를 유지하며 갱신일로 오래된 데이터임을 알 수 있게 한다.
- README는 저장소 상대 경로 이미지를 사용하고 기여 캘린더는 GitHub 프로필로 연결한다. 생성기는 README 본문을 고치지 않는다.
- `.github/workflows/grs.yml`은 하루 한 번·수동 실행·통계 코드 push 때 실행한다. 원격 토큰·브랜치 정책 검증은 실제 Actions 실행 결과로 확인한다.
- 언어 카드는 공개·본인 소유·비-fork 저장소의 주 언어별 개수다. 언어 미지정은 제외하고 archive는 포함한다. 코드 비중·숙련도를 뜻하지 않는다.

근거: https://docs.github.com/en/graphql/reference/users#contributionscollection

## 검토

사실·공개 여부·링크·SVG 유효성·실패 시 기존 이미지 보존을 확인한다. README 변경을 검토할 수 있도록 diff를 제공한다. 사용자 요청 없이 커밋·푸시하지 않는다.
